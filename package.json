{
  "scripts": {
    "build": "tsc --build && cp src/*.html dist",
    "dev": "serve dist"
  },
  "devDependencies": {
    "serve": "^14.2.4",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.8.3"
  }
}
